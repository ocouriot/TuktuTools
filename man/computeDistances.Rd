% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/computeDistances.R
\name{computeDistances}
\alias{computeDistances}
\title{Compute migration distances for a single individual-year}
\usage{
computeDistances(
  df,
  X.col = "X",
  Y.col = "Y",
  time.col = "Time",
  start = "02-01",
  end = "06-01",
  rate = 8,
  tolerance = 2,
  cushion_days = 7
)
}
\arguments{
\item{df}{A data frame with at minimum columns for longitude, latitude, and
time. Column names controlled by \code{X.col}, \code{Y.col}, and
\code{time.col}.}

\item{X.col}{Character. Name of the longitude column. Default \code{"X"}.}

\item{Y.col}{Character. Name of the latitude column. Default \code{"Y"}.}

\item{time.col}{Character. Name of the POSIXct datetime column.
Default \code{"Time"}.}

\item{start}{Character \code{"MM-DD"} giving the start of the date window
(inclusive). Default \code{"02-01"}.}

\item{end}{Character \code{"MM-DD"} giving the end of the date window
(inclusive). Default \code{"06-01"}.}

\item{rate}{Numeric. Resampling interval in hours. Default \code{8}.}

\item{tolerance}{Numeric. Tolerance around \code{rate} (in hours) passed to
\code{amt::track_resample}. Default \code{rate / 2}.}

\item{cushion_days}{number of days within the start and end within which to 
allow the calculation}

\item{...}{Additional arguments passed to \code{amt::track_resample}.}
}
\value{
Data frame with:
  \describe{
    \item{n_fixes}{Number of resampled fixes used.}
    \item{total_distance}{Total path length in kilometres.}
    \item{net_displacement}{Straight-line distance from first to last fix, in kilometres.}
  }
}
\description{
Resamples a telemetry track to a regular time interval and computes total
path length (total distance) and straight-line displacement (net distance)
over a specified date window. Designed to be called within a
\code{group_by / summarise} or \code{nest / map} workflow across IDs and years.
}
\details{
Workflow:
\enumerate{
  \item Filter rows to the \code{start}–\code{end} date window.
  \item Build an \pkg{amt} track and reproject to \code{crs_to}.
  \item Resample to \code{rate} hours (± \code{tolerance}).
  \item Compute distances using complex-number arithmetic on the projected
        coordinates (\code{z = x_ + 1i * y_}).
}
}
\examples{
require(TuktuTools)
data(caribou)

# plyr version

distances <- caribou |> ddply(c("ID", "Year"), computeDistances, 
                              X.col = "x", Y.col = "y", start = "02-01", end  = "06-01")

# dplyr version

distances <- caribou |>
    group_by(ID, Year) |>
    group_modify(~ computeDistances(.x, X.col = "x", Y.col = "y",
                                    start = "02-01", end = "06-01")) |>
    ungroup()
}
